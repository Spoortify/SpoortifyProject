<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Sportify.View.Standings"
             Title="Standings"
             xmlns:Controller="clr-namespace:Sportify.Controller"
             xmlns:Model="clr-namespace:Sportify.Model"
             Shell.NavBarIsVisible="False">
    <ContentPage.BindingContext>
        <Controller:StandingsFormula1Controller/>
    </ContentPage.BindingContext>
    <ScrollView>
        <VerticalStackLayout HorizontalOptions="Center" Margin="20">
            <HorizontalStackLayout>
                <Label Text="Season:" Margin="0,15,0,0" HorizontalOptions="Center"/>
                <Picker ItemsSource="{Binding Seasons}" Margin="20,0,0,0" WidthRequest="100" SelectedItem="{Binding Season}"
                        HorizontalOptions="Center"/>
                <Button Text="Confirm" Command="{Binding GetStandingsCommand}" HorizontalOptions="Center"/>
            </HorizontalStackLayout>
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Label Text="POSITION" TextColor="Black" HorizontalOptions="Center"/>
                <Label Text="TEAM" Grid.Column="1" HorizontalOptions="Center"/>
                <Label Text="NUMBER" Grid.Column="2" TextColor="Black" HorizontalOptions="Center"/>
                <Label Text="DRIVER" Grid.Column="3" TextColor="Black" HorizontalOptions="Center"/>
                <Label Text="POINTS" Grid.Column="4" TextColor="Black" HorizontalOptions="Center"/>
                <Label Text="GAP" Grid.Column="5" TextColor="Black" HorizontalOptions="Center"/>
            </Grid>
            <ScrollView>
                <CollectionView ItemsSource="{Binding Formula1.Response}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="Model:Response">
                            <Frame BackgroundColor="#5B5A5A">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <Label Text="{Binding Position}" TextColor="White" HorizontalOptions="Center"/>
                                    <Image Source="{Binding Team.Logo}" Grid.Column="1" HorizontalOptions="Center"/>
                                    <Label Text="{Binding Driver.Number}" Grid.Column="2" TextColor="White" HorizontalOptions="Center"/>
                                    <Label Text="{Binding Driver.Name}" Grid.Column="3" TextColor="White" HorizontalOptions="Center"/>
                                    <Label Text="{Binding Points}" Grid.Column="4" TextColor="White" HorizontalOptions="Center"/>
                                    <Label Text="{Binding Behind,StringFormat='-{0}'}" Grid.Column="5" TextColor="White" HorizontalOptions="Center"/>
                                </Grid>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </ScrollView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>